# Evidence Explorer

The investigation workspace shows the complete evidence set for the selected incident and marks which evidence IDs were selected by the diagnosis.

Use the explorer to inspect evidence content, source, and kind, then filter between all evidence, selected evidence, and unused evidence.

The explorer is a frontend-only view over the existing `/scenarios` and `/investigations` API contracts; it does not change diagnosis or evaluation behavior.
