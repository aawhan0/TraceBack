# TraceBack frontend

A focused React interface for the TraceBack investigation and evaluation service.

## Development

```bash
npm install
npm run dev
```

Set `VITE_API_URL` when the FastAPI API is not served from the same origin.
